{
  "tasks": [
    {
      "label": "build c fast",
      "type": "shell",
      "command": "g++",
      "args": [
        "-O3",
        "-g",
        "${file}",
        "-o",
        "${fileDirname}\\${fileBasenameNoExtension}.exe"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "build c default",
      "type": "shell",
      "command": "g++",
      "args": [
        "-g",
        "${file}",
        "-o",
        "${fileDirname}\\${fileBasenameNoExtension}.exe"
      ]
    }
  ],
  "version": "2.0.0"
}
